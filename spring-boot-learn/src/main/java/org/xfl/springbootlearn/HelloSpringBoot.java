package org.xfl.springbootlearn;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.beans.factory.annotation.Value;import org.springframework.boot.CommandLineRunner;import org.springframework.context.ApplicationContext;import org.springframework.context.annotation.Bean;import org.springframework.web.bind.annotation.GetMapping;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RequestMethod;import org.springframework.web.bind.annotation.RestController;import org.xfl.springbootlearn.bean.ConfigBean;import org.xfl.springbootlearn.bean.MyPropertiesBean;import java.util.Arrays;/** * @program: learn * @description: Hello Spring Boot * @author: xfl * @create: 2018-07-18 18:37 **/@RestControllerpublic class HelloSpringBoot {    /**     * 姓名     */    @Value("${her.name}")    private String name;    /**     * 年龄     */    @Value("${her.age}")    private Integer age;    /**     * 性别     */    @Value("${her.sex}")    private String sex;    @Autowired    ConfigBean configBean;    @Autowired    MyPropertiesBean myPropertiesBean;    //@RequestMapping(value = "/",method = RequestMethod.GET)    @GetMapping("/")    public String index(){        String person = "姓名:" + this.name + ";性别:" + sex + ";年龄:" + age;        return "Greetings from Spring Boot!" + configBean.toString() + "Mine:" + myPropertiesBean.toString();    }    //@Bean    public CommandLineRunner commandLineRunner(ApplicationContext context){        return args -> {            System.out.println("Let's inspect the bean provided by Spring Boot:");            String[] beanNames = context.getBeanDefinitionNames();            Arrays.sort(beanNames);            for (String beanName:beanNames) {                System.out.println(beanName);            }        };    }    @Bean    public String init(){        System.out.println("我被初始化了!");        return null;    }}